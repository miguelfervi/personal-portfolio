---
interface Props {
  items: string[];
}

const { items } = Astro.props;
---

<ul class="mt-3 flex flex-wrap gap-2">
  {
    items.map((item) => (
      <li class="rounded-full bg-muted px-3 py-1 text-sm text-foreground">{item}</li>
    ))
  }
</ul>
