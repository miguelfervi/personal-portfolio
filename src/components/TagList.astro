---
interface Props {
  tags: string[];
}

const { tags } = Astro.props;
---

<ul class="flex flex-wrap gap-2">
  {
    tags.map((tag) => (
      <li class="rounded-full bg-accent-soft px-2.5 py-1 font-mono text-xs text-foreground">{tag}</li>
    ))
  }
</ul>
