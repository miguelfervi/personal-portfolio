---
import { requestLocale, t, type Text } from "../lib/i18n";

interface Props {
  t: Text;
  class?: string;
}

const { t: text, class: className } = Astro.props;
const locale = requestLocale(Astro.currentLocale, Astro.url.pathname);
---

<span class={className}>{t(text, locale)}</span>
