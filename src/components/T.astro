---
import type { Text } from "../lib/i18n";

interface Props {
  t: Text;
  class?: string;
}

const { t, class: className } = Astro.props;
---

<span class:list={["i18n-en", className]}>{t.en}</span><span class:list={["i18n-es", className]}>{t.es}</span>
